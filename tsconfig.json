{
  "compilerOptions": {
    "baseUrl": ".",  // Points to the root of the project
    "paths": {
      "@/*": ["src/*"]  // Resolves '@' to the 'src' folder
    },
    "strict": true,  // Enables strict type checking
    "esModuleInterop": true,  // Enables compatibility with CommonJS modules
    "lib": ["dom", "es2015"],  // Standard library settings
    "jsx": "react-jsx"  // JSX handling for React 17+
  },
  "include": [
    "src/**/*"  // Include all source files under the 'src' directory
  ]
}
